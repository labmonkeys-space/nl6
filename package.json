{
  "name": "nl6-docs",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "description": "Documentation site for nl6 (Docusaurus)",
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@docusaurus/core": "3.10.2",
    "@docusaurus/faster": "3.10.2",
    "@docusaurus/preset-classic": "3.10.2",
    "@docusaurus/theme-mermaid": "3.10.2",
    "@easyops-cn/docusaurus-search-local": "^0.55.3",
    "@mdx-js/react": "^3.0.0",
    "clsx": "^2.0.0",
    "prism-react-renderer": "^2.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "3.10.2",
    "@docusaurus/tsconfig": "3.10.2",
    "@docusaurus/types": "3.10.2",
    "@types/react": "^19.0.0",
    "typescript": "~7.0.2"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  },
  "comments": {
    "overrides": "Force patched versions of transitive Docusaurus/webpack deps flagged by Dependabot. These are docs-site build/dev dependencies (not shipped in the nl6 binary or image). Every entry MUST be load-bearing (change resolution) and MUST have a removal condition in overrides-entries below; an inert entry is a version ceiling that blocks upgrades while delivering nothing. Run `make docs-audit-overrides` to find inert entries. Prefer a nested (per-consumer) override over a global one when only some consumers need the forced version.",
    "overrides-entries": {
      "serialize-javascript": "Advisory fix. Forces 7.x on copy-webpack-plugin@11 and css-minimizer-webpack-plugin@5, both of which declare ^6.x -- a deliberate major-boundary crossing. Without it the tree drops to 6.0.2. REMOVE WHEN both plugins accept ^7.",
      "sockjs>uuid": "Advisory fix for uuid 8.3.2, reached only via sockjs under webpack-dev-server (npm run start) -- never in the production build or the shipped site. Scoped to sockjs so it does not cap mermaid, which accepts ^11 || ^12 || ^13 || ^14. Forces 11.1.1 on sockjs, whose declared range is ^8.3.2. REMOVE WHEN sockjs accepts a patched uuid."
    }
  },
  "overrides": {
    "serialize-javascript": "^7.0.5",
    "sockjs": {
      "uuid": "^11.1.1"
    }
  }
}
